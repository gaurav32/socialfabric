services:
  api-server:
    build:
      context: ../..
      dockerfile: artifacts/api-server/Dockerfile
    container_name: socialfabric-api
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
